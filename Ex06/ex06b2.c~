#include<stdio.h>

int main(){
  int n,cnt;
  printf("正の整数を入力してください：");
  scanf("%d", &n);

  for(cnt=0;n==1;cnt++){
    if(n%2 == 1) n = n*3 + 1;
    else n = n/2;
  }
  printf("%d",cnt);
  return 0;
}
